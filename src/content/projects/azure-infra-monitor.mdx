---
title: "Azure Infrastructure Monitor"
category: "Simulated Cloud Monitoring Dashboard"
summary: "A Streamlit dashboard simulating Azure infrastructure monitoring with realistic mock data, surfacing VM performance, resource usage, and alerts in one view."
description: "A Streamlit dashboard simulating Azure infrastructure monitoring with realistic mock data, surfacing VM performance, storage usage, App Service health, and cost tracking without connecting to a live Azure subscription."
tech: ["Streamlit", "Python", "Mock Data", "Dashboard"]
accent: "#6366F1"
order: 7
filters: ["Cloud"]
github: "https://github.com/amna-albasher/azure-infrastructure-monitor"
role: "Built and delivered the monitoring dashboard end to end, including the Streamlit UI, mock data layer, and monitoring views."
problem: [
  "I wanted to build a dashboard that could bring Azure infrastructure health, resource usage, cost tracking, and anomaly detection into one place. The idea was to have a single view for things like VMs, storage accounts, App Services, and subscription costs, similar to the monitoring dashboards used by cloud teams."
]
approach: [
  "I built a Streamlit dashboard that displays VM performance, storage usage, App Service health, and subscription level cost tracking with budget utilization. Instead of connecting the demo directly to a live Azure subscription, I built a mock data generator that produces realistic Azure metrics. This avoids exposing Azure credentials and makes the demo safer and easier to control when presenting it. The mock data includes realistic resource names, regions, cost ranges, and performance values so the dashboard behaves like a real monitoring dashboard rather than displaying obviously fake numbers. I also added auto refresh so the dashboard updates automatically while it is open."
]
keyDecisions: [
  "Used simulated data instead of connecting directly to a live Azure subscription, so the demo does not require or expose Azure credentials.",
  "Built the mock data to look like realistic Azure resource and monitoring data, including resource names, regions, costs, and performance values.",
  "Used Streamlit to build the dashboard without having to create a separate frontend application.",
  "Added automatic refresh so the dashboard can show changing metrics while it is running."
]
architecture: "A mock data generator produces realistic Azure-style metrics, which are rendered in a Streamlit dashboard showing VM performance, storage usage, App Service health, and cost tracking."
results: [
  "A working dashboard showing VM performance, storage usage, App Service health, subscription costs, budget utilization, and anomaly information.",
  "A credential free demo that can be presented safely without connecting to a real Azure subscription.",
  "Auto refreshing views that make the simulated monitoring data feel closer to a live cloud monitoring dashboard."
]
draft: false
terminalCommand: "run azure-infra-monitor"
terminalLines: ["metrics synced", "dashboard live"]
---

Azure Infrastructure Monitor is a Streamlit dashboard simulating Azure infrastructure monitoring with realistic mock data, surfacing resource health, cost tracking, and anomaly information without connecting to a live Azure subscription.
