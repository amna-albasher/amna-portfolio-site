---
title: "AI Incident Response System"
category: "AI-Powered Incident Diagnosis"
summary: "A Flask app with a Python log watcher that detects errors in Docker logs in real time and sends them to the Claude API, which returns a plain-language incident report."
description: "A Flask app with a Python log watcher that detects errors in Docker logs in real time and sends them to the Claude API, which returns a plain-language incident report explaining what likely happened and where to look first."
tech: ["Flask", "Claude API", "Python", "Docker"]
accent: "#14B8A6"
order: 4
filters: ["AI", "DevOps"]
github: "https://github.com/amna-albasher/ai-incident-response"
role: "Built the whole detection-to-diagnosis flow myself, the Flask app, the log watcher, and the AI piece that ties it together."
problem: [
  "When something breaks, the hard part usually isn't noticing it, logs will tell you that much. The real delay is between an error showing up and someone actually figuring out what it means. I wanted to shrink that gap so a diagnosis starts forming the moment something goes wrong, not ten minutes later once someone opens the logs."
]
approach: [
  "I built a small Flask app with endpoints that simulate real failures, then wrote a Python script that watches the app's Docker logs as they come in. The moment it spots an error line, it grabs the surrounding log context and sends it to the Claude API, which comes back with a plain-language report on what probably happened and where to look first."
]
keyDecisions: [
  "Watched the container logs directly instead of pulling from a separate monitoring tool, so nothing sits in a queue waiting to be checked.",
  "Used the Claude API instead of a fixed list of known error types, since a static lookup can't explain something it hasn't seen before, and errors rarely repeat exactly.",
  "Made the whole thing automatic. An error shows up, a report comes out. No step where someone has to trigger anything by hand."
]
architecture: "A Python log watcher tails the Flask app's Docker logs, and on detecting an error, sends the surrounding context to the Claude API, which returns a plain-language incident report."
results: [
  "A pipeline that goes from a simulated failure to an AI-written incident report without anyone touching it in between.",
  "A report that actually reads like something useful, not a stack trace, something a person could act on right away."
]
draft: false
terminalCommand: "deploy incident-response"
terminalLines: ["log watcher active", "Claude API connected"]
---

The AI Incident Response System is a Flask app with a Python log watcher that detects errors in Docker logs in real time and sends them to the Claude API for a plain-language incident report.
