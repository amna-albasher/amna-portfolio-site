---
title: "Smart Infrastructure Advisor"
category: "AI Architecture Recommendations"
summary: "A FastAPI application powered by the Claude API that analyzes cloud workload requirements and recommends optimal Azure architecture with real-time pricing estimates. Live on Vercel."
description: "A FastAPI application powered by the Claude API that analyzes cloud workload requirements and recommends optimal Azure architecture with real-time pricing estimates. Live on Vercel."
tech: ["FastAPI", "Claude API", "Python", "Vercel", "Azure"]
accent: "#E0367A"
order: 6
filters: ["AI", "Cloud"]
github: "https://github.com/amna-albasher/smart-infrastructure-advisor"
liveDemo: "https://smart-infrastructure-advisor.vercel.app"
problem: [
  "Choosing the right Azure architecture for a given workload often comes down to individual experience and guesswork. Balancing compute, storage, and networking choices against cost is a genuinely hard tradeoff to reason through quickly, especially for less common workload shapes."
]
approach: [
  "Built a FastAPI application powered by the Claude API that takes in stated workload requirements and returns a recommended Azure architecture along with real-time pricing estimates, deployed live on Vercel so it's usable as a real tool rather than a local script."
]
keyDecisions: [
  "FastAPI was chosen for a lightweight, fast-to-iterate backend that could expose the recommendation logic as a clean API.",
  "Real-time pricing estimates were included specifically so recommendations come with a cost tradeoff attached, not just a technical suggestion in isolation.",
  "Deployed live rather than kept as a notebook or CLI tool, since the goal was a genuinely usable advisor, not a proof of concept."
]
architecture: "Workload requirements are submitted to a FastAPI backend, analyzed by the Claude API, and returned as an architecture recommendation with a pricing estimate, served on Vercel."
results: [
  "Turns an architecture decision that normally requires research and back-and-forth into an immediate, data-backed recommendation.",
  "Cost visibility is built into the recommendation itself, not a separate step.",
  "Publicly usable via a live demo rather than requiring local setup."
]
draft: false
terminalCommand: "run infra-advisor"
terminalLines: ["resource scan complete", "recommendations generated"]
---

Smart Infrastructure Advisor is a FastAPI application powered by the Claude API that analyzes cloud workload requirements and recommends optimal Azure architecture with real-time pricing estimates.
